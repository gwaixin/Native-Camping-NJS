/* set dependencies */
var connect = require('./connect.js');

/* export teacher functions */
module.exports = {
	registerTeacher: function(obj, resolve, reject, socket) {
		console.log("testing register teacher");
	}
};